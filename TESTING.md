# v0.2 实机测试指南

本指南用于 PR #1 合并前的 Windows ↔ Android 局域网同步验证。

## 1. 下载测试包

打开仓库 **Actions** → **v0.2 test builds** → 最新一次成功的运行。

在页面底部 **Artifacts** 下载：

- `family-birthday-windows-v0.2-test-...`
- `family-birthday-apk-v0.2-test-...`

Windows 构建包解压后运行其中的程序。Android 构建包解压后，把 APK 安装到手机。

> Android 测试 APK 使用开发/调试签名，只用于侧载测试，不用于应用商店发布。

## 2. 先做单机功能测试

电脑和手机分别测试：

- 添加公历生日，例如：`测试甲 公历10月1日`
- 添加农历生日，例如：`测试乙 农历腊月二十`
- 编辑记录
- 删除记录
- 关闭应用后重新打开，确认数据仍存在

如果这里就失败，先不要测试同步。

## 3. Windows 作为主机

1. 电脑和手机连接同一个 Wi-Fi / 局域网。
2. Windows 打开应用 → **局域网双向同步**。
3. 点 **启动本机同步服务**。
4. Windows 第一次可能弹出防火墙提示：
   - 允许 **专用网络**；
   - 校园网如果被 Windows 识别成“公用网络”，测试时可能需要同时允许公用网络，测试结束后可再收紧。
5. 记下界面显示的：
   - 局域网地址（例如 `192.168.1.23:54321`）
   - 6 位配对码。

## 4. Android → Windows 双向同步

先制造两端不同数据：

- Windows 新增：`电脑测试 公历1月2日`
- Android 新增：`手机测试 公历3月4日`

然后在 Android：

1. 打开 **局域网双向同步**。
2. 在“连接另一台设备”输入 Windows 显示的地址。
3. 输入 6 位配对码。
4. 点 **双向同步**。

预期结果：

- 手机出现“同步成功”；
- 手机同时有“电脑测试”和“手机测试”；
- Windows 列表刷新后也同时存在这两条记录。

## 5. 删除同步测试

1. Windows 删除“电脑测试”。
2. 再执行一次 Android → Windows 双向同步。
3. 两端都不应再出现“电脑测试”。

这一步用于验证 tombstone 删除标记不会被旧数据“复活”。

## 6. 二维码备用路径

在 Windows 主机同步窗口中，用手机系统相机扫描二维码。

预期：

- 打开的地址是 `http://192.168.../` 之类的私有局域网地址；
- 页面标题为“家庭生日管理器 · 局域网同步”；
- 可以下载电脑 JSON 备份；
- 可以选择手机之前导出的 JSON 上传并合并；
- 合并后会下载新的合并备份 JSON。

## 7. 反向测试：Android 作为主机

交换角色再测试一次：

1. Android 启动本机同步服务。
2. Windows 输入手机显示的地址和配对码。
3. Windows 点双向同步。

如果手机显示的地址是 `127.0.0.1`，说明 Android 端局域网 IP 自动发现需要继续修复，请记录这一现象。

## 8. 校园网特别测试

校园网经常开启 **AP / Client Isolation（客户端隔离）**。

如果两端都能上网、也处于同一个 Wi-Fi，但同步一直提示无法连接：

1. 不要先判断程序坏了；
2. 用手机热点测试一次：
   - 手机开热点；
   - 电脑连接手机热点；
   - Windows 作为主机再测试。
3. 如果热点下成功、校园网下失败，基本可以判定是校园网阻止设备互访。

## 9. Clash / v2rayN 测试

电脑代理可以保持开启再测试一次。

v0.2 客户端代码会主动绕过 HTTP 系统代理，因此预期局域网同步仍应成功。

如果“代理关掉成功、打开失败”，请记录：

- 使用的软件；
- 系统代理是否开启；
- TUN 模式是否开启。

## 10. 需要反馈的信息

遇到失败时，请记录：

- 哪台设备作为主机；
- 两端显示的局域网地址；
- Windows 防火墙是否放行；
- 使用校园网 / 家庭 Wi-Fi / 手机热点；
- 应用界面完整报错文字；
- 是否开启 Clash / v2rayN / TUN；
- Android 扫二维码是否能打开局域网页面。

不要在公开 Issue 中上传真实家庭生日 JSON。


## 11. Windows Win+V 剪贴板历史回归测试

Windows 端打开应用并点入“快速添加”输入框：

1. 先复制两段不同的测试文本，让 Windows 剪贴板历史中至少有两项。
2. 按 `Win+V`。
3. 选择一条**不是当前最新剪贴板**的历史文本。
4. 预期：选中的文字会自动出现在“快速添加”输入框。新版使用 Win32 按键状态 + Clipboard Sequence Number 轮询，不依赖 Flutter 是否收到 Win+V。
5. 再测试普通 `Ctrl+V`，应继续正常。
6. 点击输入框右侧的“从系统剪贴板粘贴”图标，也应能读取当前剪贴板文本。

如果第 4 步仍失败，请记录：
- Win+V 面板选中内容后，应用窗口是否重新获得焦点；
- 右侧“粘贴”按钮是否正常；
- 普通 Ctrl+V 是否仍正常。


## 12. Windows 完全退出 / 文件句柄释放测试

1. 启动 Windows 版应用。
2. 至少执行一次“局域网双向同步” → “启动本机同步服务”。
3. 不必关闭同步窗口，直接点击应用右上角 **X**。
4. 等待约 1 秒。
5. 打开任务管理器，确认不存在 `family_birthday_manager.exe` 残留进程。
6. 直接删除整个 Windows 测试文件夹。

预期：
- 应用窗口关闭后进程完全退出；
- 不再有后台网络线程持有可执行文件或 DLL；
- 测试文件夹可以立即删除，不需要 File Locksmith / 任务管理器强制结束。

如果失败，请记录：
- 是否曾启动局域网同步服务；
- 任务管理器中残留进程数量；
- 等待 5 秒后进程是否自行消失；
- File Locksmith 显示的占用进程名称。


## 13. v0.3 本地通知实机测试

### Windows

1. 打开“提醒设置”。
2. 开启“应用自己的本地通知”。
3. 设置提醒时间，并点“发送测试通知”。
4. 应立即收到 Windows Toast。
5. 保存提醒设置。
6. 新建一个用于测试的生日，保留默认“提前 7 天 / 1 天 / 当天”。
7. 关闭应用，确认任务管理器中没有残留进程。
8. 对短时间触发测试，可临时把测试生日/提醒时间调整到接近当前时间后重新刷新，确认**应用关闭后** Windows 仍能弹出 Scheduled Toast。

重点：
- 注册提醒后程序不应常驻；
- Win+V 和局域网退出清理不能回归；
- 关闭本地提醒后，已注册的 Windows Scheduled Toast 应被清除。

### Android

1. 安装 v0.3 测试 APK。
2. 打开“提醒设置”。
3. 开启本地通知；Android 13+ 应弹出系统通知权限请求。
4. 授权后点“发送测试通知”，应立即收到通知。
5. 保存设置，关闭 App。
6. 对短时间触发测试，创建接近当前时间的测试提醒，确认 App 不在前台时仍能收到。
7. 重启手机后再次确认后续计划仍存在/能触发。

重点：
- 不授予权限时，应用应自动保持“本地提醒关闭”，ICS 不受影响；
- 关闭本地提醒后应取消本应用计划；
- Android 端不应要求前台常驻服务。

### ICS 回归

无论应用本地通知是否开启：

- “导出 20 年 ICS”仍应正常；
- 每个人编辑页中取消某个提醒后，ICS 不应再生成对应 VALARM；
- 三个提醒全部取消时，生日事件仍存在，但不应附带默认提醒。
